#ifndef __VOLUME_KU_H__
#define __VOLUME_KU_H__

#include <stdio.h>

int volumeOfCone(float radius, float height);
int volumeOfSphere(float radius);
int volumeOfCylinder(float radius,float height);

#endif